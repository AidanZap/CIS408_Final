import React from 'react';
import { Button, Container } from "@mui/material";

function App() {
  return <Container>
    <Button variant='contained'>Look at this button</Button>
  </Container>;
}

export default App;
